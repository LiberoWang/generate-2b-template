import './tablet.scss';

const App = () => {
  return (
    <div className='container'>
      Hello React in CMS.
    </div>
  );
};

window.ReactDOM.render(<App />, document.getElementById('ofp-2b-demo-tablet'));
